{
  "snapshot": "POPULATED_FROM_SETTINGS",
  "install": "pip install numpy requests livekit livekit-api playwright && python3 -m playwright install chromium && apt-get update -qq && apt-get install -y -qq openssh-client && bash \"project 1/25/.cursor/setup_ssh.sh\"",
  "start": "",
  "terminals": []
}
